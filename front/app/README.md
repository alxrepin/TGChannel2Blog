# Структура приложения

Приложение разбито на модули и компоненты согласно принципам Clean Architecture и Domain-Driven Design.

## 📁 Структура директорий

```
app/
├── modules/                    # Доменные модули (бизнес-логика)
│   ├── channel/               # Модуль канала
│   │   ├── types.ts          # TypeScript типы для канала
│   │   └── components/       # Компоненты канала
│   │       ├── ChannelAvatar.vue
│   │       ├── ChannelInfo.vue
│   │       └── ChannelStats.vue
│   │
│   └── posts/                # Модуль постов
│       ├── types.ts          # TypeScript типы для постов
│       └── components/       # Компоненты постов
│           ├── PostCard.vue
│           ├── PostContent.vue
│           └── PostNavigation.vue
│
├── components/               # Переиспользуемые компоненты
│   ├── ui/                  # Атомарные UI компоненты
│   │   ├── UiButton.vue
│   │   ├── UiCard.vue
│   │   └── UiContainer.vue
│   │
│   └── layout/              # Компоненты макета
│       ├── AppHeader.vue
│       ├── AppFooter.vue
│       └── NavBar.vue       # Навигационная панель
│
├── features/                # Функциональные компоненты
│   ├── SubscribeButton.vue  # Кнопка подписки с логикой
│   ├── PostGrid.vue         # Сетка постов
│   └── RelatedPost.vue      # Связанный пост
│
├── composables/             # Переиспользуемая логика
│   ├── useChannelData.ts    # Данные канала
│   └── usePostsData.ts      # Данные и функции постов
│
├── pages/                   # Страницы (роутинг)
│   ├── index.vue            # Главная страница
│   └── posts/
│       └── [post].vue       # Страница поста
│
└── app.vue                  # Корневой компонент
```

## 🎯 Принципы организации

### Модули (modules/)
- **Назначение**: Инкапсуляция бизнес-логики и доменных сущностей
- **Содержит**: TypeScript типы, компоненты, связанные с конкретным доменом
- **Примеры**: `channel`, `posts`

### UI компоненты (components/ui/)
- **Назначение**: Переиспользуемые атомарные компоненты
- **Особенности**: Не содержат бизнес-логики, только UI
- **Примеры**: `UiButton`, `UiCard`, `UiContainer`

### Layout компоненты (components/layout/)
- **Назначение**: Структурные блоки макета
- **Примеры**: `AppHeader`, `AppFooter`

### Features (features/)
- **Назначение**: Компоненты с конкретной функциональностью
- **Особенности**: Могут содержать бизнес-логику и составлять композицию из других компонентов
- **Примеры**: `SubscribeButton`, `PostGrid`

### Pages (pages/)
- **Назначение**: Только роутинг и композиция компонентов
- **Особенности**: Минимум логики, максимум композиции

## 🚀 Используемые технологии

- **Nuxt 3** - SSR framework
- **TypeScript** - типизация
- **Master CSS** - utility-first CSS
- **Composition API** - Vue 3 API

## 📊 SEO оптимизация

Реализованы следующие SEO практики:

- ✅ Семантические HTML теги (`header`, `footer`, `section`, `article`)
- ✅ Meta теги (description, keywords, author)
- ✅ Open Graph теги для социальных сетей
- ✅ Twitter Card теги
- ✅ Canonical ссылки
- ✅ Alt теги для изображений
- ✅ Lazy loading для изображений
- ✅ Semantic HTML структура
- ✅ Правильные heading теги (h1, h2)

## 🎨 Master CSS Best Practices

Используются utility-классы Master CSS:

- **Layout**: `flex`, `grid`, `ai:center`, `jc:space-between`
- **Sizing**: `w:full`, `max-w:lg`, `h:100%`
- **Spacing**: `p:16`, `m:20`, `gap:8`
- **Typography**: `f:24`, `f:bold`, `lh:1.6`
- **Colors**: `bg:sky-2`, `color:fade-50`
- **Responsive**: `grid-cols:1`, `grid-cols:2@md`, `grid-cols:3@2xl`
- **Effects**: `box-shadow:0|0|20|fade-88`, `r:24`

## 📝 Типизация

Все компоненты и модули полностью типизированы с использованием TypeScript:

```typescript
// Пример: modules/channel/types.ts
export interface ChannelInfo {
  name: string
  description: string
  avatar: string
  subscriberCount: number
  telegramHandle: string
  blogUrl: string
}

// Пример: modules/posts/types.ts
export interface Post {
  id: string
  title: string
  excerpt: string
  publishedAt: string
  thumbnail?: string
  hasImage: boolean
}